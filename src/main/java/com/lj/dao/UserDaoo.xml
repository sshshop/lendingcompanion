<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.lj.dao.UserDao">
    <!--跟数据库中的表达成映射关系-->
    <resultMap id="tuser" type="com.lj.vo.User">
        <id column="id" property="id"/>
        <result column="name" property="name"/>
        <result column="pwd" property="pwd"/>
    </resultMap>
    <!--userdao中有注解，这儿就不需要设置了-->
  <!--  <select id="selectById" resultMap="com.lj.vo.User" parameterType="java.lang.String">
         select * from TUSER t where t.id = #{id}
    </select>-->
</mapper>